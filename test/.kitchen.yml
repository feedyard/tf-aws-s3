---
driver:
  name: "terraform"
  root_module_directory: "test/fixture"
  variable_files:
    - test/fixture/test_environment.json

provisioner:
  name: "terraform"

platforms:
- name: "aws"

verifier:
  name: "awspec"

suites:
- name: "default"
  verifier:
    name: "awspec"
    patterns:
    - "test/integration/default_spec.rb"